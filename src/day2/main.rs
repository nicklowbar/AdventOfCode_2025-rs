use anyhow::Result;
use shared::shared_main;
use std::fs::File;
fn main() -> Result<()> {
    shared_main(solution1, solution2)
}

fn solution1(_input: &File) -> Result<u32> {
    let solution = 0;

    Ok(solution)
}

fn solution2(_input: &File) -> Result<u32> {
    let solution = 0;

    Ok(solution)
}
